# Antigravity Rules
# 这里是 AI 的行为准则配置，你可以随时修改此文件来调整我的行为。

# 1. 语言偏好 (Language Preference)
User prefers responses in: Chinese (Simplified)
指令：始终用简体中文回复，除非被要求翻译或解释英文术语。

# 2. 代码风格 (Code Style)
# 在下方添加你的代码风格偏好
# 例如：
# - "使用 TypeScript 而不是 JavaScript"
# - "优先使用箭头函数"
# - "代码注释请使用英文"

# 3. 行为准则 (Behavioral Rules)
# 在下方添加你希望我遵守的行为模式
# 例如：
# - "在修改代码前，先解释你的计划"
# - "回答要简洁，不要废话"

# 4. 文件安全 (File Safety)
单一备份策略：修改文件前，始终用当前文件内容覆盖旧备份（如 `filename.bak`），只保留修改前的这一个版本。除非我另有要求，否则不要创建多个历史备份。
(Single Backup Policy: Always overwrite the existing backup (e.g., `filename.bak`) with the current file content before modification. Maintain only the immediate previous version unless multiple backups are explicitly requested.)



